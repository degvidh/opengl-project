# version 330 core

layout (lcoation=0) in vec2 vertexPos;
layout (lcoation=1) in vec3 vertexColor;

out vec3 fragmnetColor;

void main() {

    gl_Position = vec4(vertexPos, 0.0, 1.0);
    fragmnetColor = vertexColor;

}